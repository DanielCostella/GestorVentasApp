# 📐 DIAGRAMAS Y FLUJOS VISUALES

## 🏗️ ARQUITECTURA ANTERIOR vs NUEVA

### ANTES: Arquitectura Flat (Plana)

```
┌─────────────────────────────────────────────────────────────┐
│                        COMPONENTES                          │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐     │
│  │FormLogin│  │TablaProds│  │ Carrito  │  │ Navbar  │     │
│  │         │  │          │  │          │  │         │     │
│  │ ❌ axios │  │ ❌ axios  │  │ ❌ axios  │  │❌session│     │
│  │ directo │  │ directo  │  │ directo  │  │ Storage │     │
│  │         │  │          │  │          │  │         │     │
│  │❌useState│  │❌useEffect│  │❌useEffect│  │❌manual │     │
│  └────┬────┘  └─────┬────┘  └─────┬────┘  └────┬────┘     │
└───────┼─────────────┼─────────────┼────────────┼───────────┘
        │             │             │            │
        │             │             │            │
        ▼             ▼             ▼            ▼
   ┌────────────────────────────────────────────────┐
   │          sessionStorage (manual)               │
   │  ❌ No reactivo                                │
   │  ❌ Cada componente lee manualmente            │
   │  ❌ No hay sincronización                      │
   └────────────────────────────────────────────────┘
```

**PROBLEMAS:**
- ❌ Código duplicado (axios en cada componente)
- ❌ No hay cache (siempre pide al servidor)
- ❌ sessionStorage no es reactivo
- ❌ Difícil de mantener

---

### AHORA: Arquitectura Basada en Features

```
┌─────────────────────────────────────────────────────────────────┐
│                         COMPONENTES                             │
│  ┌─────────┐    ┌──────────┐    ┌──────────┐    ┌─────────┐   │
│  │FormLogin│    │TablaProds│    │ Carrito  │    │ Navbar  │   │
│  │         │    │          │    │          │    │         │   │
│  │✅useAuth│    │✅useProds│    │✅useVentas│   │✅useAuth│   │
│  └────┬────┘    └─────┬────┘    └─────┬────┘    └────┬────┘   │
└───────┼──────────────┼──────────────┼──────────────┼──────────┘
        │              │              │              │
        │              │              │              │
        ▼              ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        CUSTOM HOOKS                             │
│  ┌─────────┐    ┌────────────┐    ┌─────────────┐              │
│  │useAuth()│    │useProductos│    │ useVentas() │              │
│  │         │    │     ()     │    │             │              │
│  │✅ Lógica│    │            │    │             │              │
│  │  reutil.│    │            │    │             │              │
│  └────┬────┘    └─────┬──────┘    └──────┬──────┘              │
└───────┼──────────────┼───────────────────┼─────────────────────┘
        │              │                   │
        ├──────────────┴───────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ESTADO (Zustand + React Query)               │
│                                                                 │
│  ┌─────────────────┐              ┌──────────────────┐         │
│  │   authStore     │              │  React Query     │         │
│  │   (Zustand)     │              │   (Cache)        │         │
│  │                 │              │                  │         │
│  │ ✅ Reactivo     │              │ ✅ Auto-cache    │         │
│  │ ✅ Persiste     │              │ ✅ Auto-retry    │         │
│  │ ✅ DevTools     │              │ ✅ DevTools      │         │
│  └────────┬────────┘              └────────┬─────────┘         │
└───────────┼──────────────────────────────┼───────────────────┘
            │                              │
            ▼                              ▼
    ┌───────────────┐            ┌──────────────────┐
    │ sessionStorage│            │  apiClient       │
    │ (automático)  │            │  (Axios)         │
    └───────────────┘            └────────┬─────────┘
                                          │
                                          ▼
                                  ┌──────────────┐
                                  │   BACKEND    │
                                  │ :8000        │
                                  └──────────────┘
```

**VENTAJAS:**
- ✅ Código reutilizable (hooks)
- ✅ Cache automático (menos peticiones)
- ✅ Estado reactivo (Zustand)
- ✅ Fácil de mantener

---

## 🔄 FLUJO DE LOGIN DETALLADO

### ANTES: Manual y No Reactivo

```
┌────────────┐
│  Usuario   │
│   ingresa  │
│ credenciales│
└──────┬─────┘
       │
       ▼
┌─────────────────────┐
│   handleSubmit()    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  axios.post()       │ ← ❌ Directo (no reutilizable)
│  URL_LOGIN          │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ if (res.status)     │ ← ❌ Manejo manual
│   if/else anidados  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ sessionStorage      │ ← ❌ No reactivo
│   .setItem()        │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  navigate("/")      │
└─────────────────────┘
       │
       ▼
┌─────────────────────┐
│ OTROS COMPONENTES   │
│ ❌ NO se actualizan │ ← Tienen que leer sessionStorage manualmente
│    automáticamente  │
└─────────────────────┘
```

---

### AHORA: Automático y Reactivo

```
┌────────────┐
│  Usuario   │
│   ingresa  │
│ credenciales│
└──────┬─────┘
       │
       ▼
┌─────────────────────┐
│   handleSubmit()    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  useAuth().login()  │ ← ✅ Hook reutilizable
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ loginMutation       │ ← ✅ React Query (auto-loading, auto-error)
│   .mutate()         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ authApi.loginUser() │ ← ✅ API centralizada
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ apiClient.post()    │ ← ✅ Interceptors automáticos
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│   BACKEND           │
│ POST /login         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│   Response          │
│ { empleado,         │
│   sucursal }        │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ onSuccess callback  │ ← ✅ Manejo automático
│ en useAuth()        │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ authStore.login()   │ ← ✅ Zustand (reactivo)
│   (Zustand)         │
└──────┬──────────────┘
       │
       ├──────────────────┐
       │                  │
       ▼                  ▼
┌──────────────┐   ┌─────────────────┐
│ sessionStorage│   │ TODOS los comp. │ ← ✅ Se actualizan automáticamente
│ (automático) │   │ con useAuth()   │
└──────────────┘   │ se RE-RENDERIZAN│
                   └─────────────────┘
       │
       ▼
┌─────────────────────┐
│  navigate("/")      │
└─────────────────────┘
```

---

## 📊 FLUJO DE DATOS: ZUSTAND + REACT QUERY

```
┌─────────────────────────────────────────────────────────────┐
│                        COMPONENTES                          │
│                                                             │
│  FormLogin    TablaProductos    CarritoVentas    Navbar    │
│     │              │                  │              │      │
│     │ useAuth()    │ useProductos()   │ useVentas()  │ useAuth()
│     ▼              ▼                  ▼              ▼      │
└─────┼──────────────┼──────────────────┼──────────────┼─────┘
      │              │                  │              │
      │              │                  │              │
┌─────▼──────────────▼──────────────────▼──────────────▼─────┐
│                     CUSTOM HOOKS                            │
│                                                             │
│  useAuth()        useProductos()        useVentas()        │
│     │                  │                    │              │
│     │                  │                    │              │
│     ├──────────────────┼────────────────────┘              │
│     │                  │                                   │
│     ▼                  ▼                                   │
│ ┌────────┐      ┌─────────────┐                           │
│ │Zustand │      │React Query  │                           │
│ │(global)│      │(server state│                           │
│ └───┬────┘      └──────┬──────┘                           │
└─────┼──────────────────┼────────────────────────────────┘
      │                  │
      │                  │
      ▼                  ▼
┌──────────────┐   ┌────────────────┐
│sessionStorage│   │   apiClient    │
│(persist)     │   │   (Axios)      │
└──────────────┘   └────────┬───────┘
                            │
                            ▼
                     ┌─────────────┐
                     │   BACKEND   │
                     │   :8000     │
                     └─────────────┘
```

---

## 🔍 DIAGRAMA: REACT QUERY CACHE

```
┌──────────────────────────────────────────────────────────┐
│             PRIMERA VEZ (sin cache)                      │
└──────────────────────────────────────────────────────────┘

Componente usa useProductos()
      │
      ▼
React Query verifica cache
      │
      ▼
   ❌ NO existe
      │
      ▼
Hace petición al servidor
      │
      ▼
┌─────────────┐
│   BACKEND   │ ← Petición HTTP
└──────┬──────┘
       │
       ▼
Guarda en cache (staleTime: 5min)
       │
       ▼
Retorna datos al componente


┌──────────────────────────────────────────────────────────┐
│        SEGUNDA VEZ (dentro de 5 minutos)                │
└──────────────────────────────────────────────────────────┘

Componente usa useProductos()
      │
      ▼
React Query verifica cache
      │
      ▼
   ✅ EXISTE y es FRESCO
      │
      ▼
Retorna desde cache (INSTANTÁNEO)
      │
      ▼
❌ NO hace petición al servidor


┌──────────────────────────────────────────────────────────┐
│      DESPUÉS DE 5 MINUTOS (stale)                       │
└──────────────────────────────────────────────────────────┘

Componente usa useProductos()
      │
      ▼
React Query verifica cache
      │
      ▼
   ⚠️ EXISTE pero es VIEJO (stale)
      │
      ▼
Retorna cache (para mostrar algo)
      │
      ▼
Hace petición al servidor en background
      │
      ▼
Actualiza cache con nuevos datos
      │
      ▼
Componente se re-renderiza con datos nuevos
```

---

## 🎯 DIAGRAMA: ZUSTAND PERSIST

```
┌──────────────────────────────────────────────────────────┐
│                   USUARIO HACE LOGIN                     │
└──────────────────────────────────────────────────────────┘

useAuth().login({ Usuario, Contraseña })
      │
      ▼
authStore.login(empleado, sucursal)
      │
      ├───────────────────────┐
      │                       │
      ▼                       ▼
┌──────────────┐      ┌─────────────────┐
│ MEMORIA      │      │ sessionStorage  │
│ (reactivo)   │      │ (persist)       │
│              │      │                 │
│ empleado: {} │      │ auth-storage:   │
│ sucursal: {} │      │ {               │
│              │      │   state: {...}  │
│              │      │ }               │
└──────┬───────┘      └─────────────────┘
       │
       │ ✅ TODOS los componentes con useAuth()
       │    se RE-RENDERIZAN automáticamente
       │
       ▼
┌──────────────────────────────────────┐
│ FormLogin    Navbar    Carrito       │
│    ▼           ▼          ▼          │
│ Se actualizan automáticamente        │
└──────────────────────────────────────┘


┌──────────────────────────────────────────────────────────┐
│              USUARIO RECARGA LA PÁGINA                   │
└──────────────────────────────────────────────────────────┘

Zustand inicializa
      │
      ▼
Lee sessionStorage (persist middleware)
      │
      ▼
Restaura estado en memoria
      │
      ▼
✅ Usuario sigue logueado
```

---

## 🛠️ DIAGRAMA: AXIOS INTERCEPTORS

```
┌──────────────────────────────────────────────────────────┐
│              COMPONENTE HACE PETICIÓN                    │
└──────────────────────────────────────────────────────────┘

apiClient.post('/empleados/login', {...})
      │
      ▼
┌─────────────────────────────────────────────────────────┐
│           REQUEST INTERCEPTOR                           │
│                                                         │
│  1. Loguea la petición                                 │
│     console.log(`[API Request] POST /empleados/login`) │
│                                                         │
│  2. Agrega headers (si es necesario)                   │
│                                                         │
│  3. Continúa con la petición                           │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
              ┌──────────────┐
              │   BACKEND    │
              │   :8000      │
              └───────┬──────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│           RESPONSE INTERCEPTOR                          │
│                                                         │
│  ✅ Si es éxito (200):                                 │
│     - Loguea respuesta                                 │
│     - Retorna datos                                    │
│                                                         │
│  ❌ Si es error (401, 403, 404, 500):                  │
│     - Loguea error                                     │
│     - Muestra SweetAlert automático                    │
│     - Retorna Promise.reject()                         │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
              Componente recibe respuesta
```

---

## 📂 DIAGRAMA: FEATURE-BASED STRUCTURE

```
features/
│
├── auth/                    ← Feature 1: Autenticación
│   ├── api/
│   │   └── authApi.js      ← Llamadas al backend
│   ├── hooks/
│   │   └── useAuth.js      ← Lógica reutilizable
│   └── components/
│       └── FormLogin.jsx   ← UI del login
│
├── productos/               ← Feature 2: Productos
│   ├── api/
│   │   └── productosApi.js
│   ├── hooks/
│   │   └── useProductos.js
│   └── components/
│       ├── TablaProductos.jsx
│       └── BotonAgregarProducto.jsx
│
└── ventas/                  ← Feature 3: Ventas
    ├── api/
    │   └── ventasApi.js
    ├── hooks/
    │   └── useVentas.js
    └── components/
        ├── CarritoVentas.jsx
        └── ListadoVentas.jsx


✅ TODO relacionado con AUTH está en auth/
✅ TODO relacionado con PRODUCTOS está en productos/
✅ TODO relacionado con VENTAS está en ventas/

VENTAJA: Fácil de encontrar, mantener y escalar
```

---

## 🔄 COMPARACIÓN: PETICIÓN HTTP

### ANTES: Manual

```
Component
    │
    └─── useEffect()
            │
            └─── try/catch
                    │
                    ├─── setLoading(true)
                    │
                    ├─── axios.get('/productos')
                    │
                    ├─── setProductos(data)
                    │
                    ├─── setLoading(false)
                    │
                    └─── catch → setError(error)

❌ 10+ líneas de código
❌ Código duplicado en cada componente
❌ Sin cache (siempre pide al servidor)
```

### AHORA: Automático

```
Component
    │
    └─── useProductos()
            │
            └─── useQuery()
                    │
                    ├─── ✅ Auto-loading
                    ├─── ✅ Auto-error
                    ├─── ✅ Auto-retry
                    ├─── ✅ Auto-cache
                    └─── ✅ Auto-revalidation

✅ 1 línea de código
✅ Reutilizable en cualquier componente
✅ Cache automático (menos peticiones)
```

---

## 📊 TIMELINE: CICLO DE VIDA DE UNA PETICIÓN

```
T=0s    Usuario hace click en "Ver Productos"
        │
        ▼
T=0.01s useProductos() se ejecuta
        │
        ▼
T=0.02s React Query verifica cache
        │
        ├─── ✅ SI existe y es fresco (< 5min)
        │    │
        │    └─── RETORNA desde cache (INSTANTÁNEO)
        │
        └─── ❌ NO existe o es viejo
             │
             ▼
T=0.05s  React Query hace petición a backend
             │
             ▼
T=0.10s  apiClient interceptor loguea request
             │
             ▼
T=0.50s  Backend procesa petición
             │
             ▼
T=1.00s  Backend responde con datos
             │
             ▼
T=1.05s  apiClient interceptor loguea response
             │
             ▼
T=1.10s  React Query guarda en cache
             │
             ▼
T=1.15s  Componente se re-renderiza con datos
             │
             ▼
T=1.20s  Usuario ve los productos
```

---

## 🎯 RESUMEN VISUAL

```
╔═══════════════════════════════════════════════════════════╗
║                    ANTES vs AHORA                         ║
╠═══════════════════════════════════════════════════════════╣
║                                                           ║
║  ANTES                        AHORA                       ║
║  ├─ Flat                      ├─ Feature-based           ║
║  ├─ useState manual           ├─ Zustand reactivo        ║
║  ├─ useEffect manual          ├─ React Query auto        ║
║  ├─ axios directo             ├─ apiClient con hooks     ║
║  ├─ sessionStorage manual     ├─ persist middleware      ║
║  ├─ Sin cache                 ├─ Cache inteligente       ║
║  ├─ Código duplicado          ├─ Código reutilizable     ║
║  ├─ Difícil de testear        ├─ Fácil de testear        ║
║  └─ No escalable              └─ Escalable               ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
```

